import { SafeAreaView, Text, StatusBar } from "react-native";

export default function SplashScreen(){
    return(
        <SafeAreaView>
            <Text>Loading...</Text>
        </SafeAreaView>
    );
}